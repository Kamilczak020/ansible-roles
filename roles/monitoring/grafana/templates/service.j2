apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: {{ monitoring_namespace }}
  annotations:
      prometheus.io/scrape: 'true'
      prometheus.io/port:   '3000'
spec:
  selector: 
    app: grafana
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      